{
  "_comment": "Single-tool contract: tools/list advertises agentstack.execute only; tools/call accepts this name.",
  "tool": "agentstack.execute",
  "arguments": {
    "params": {
      "steps": [
        { "action": "hosting.publish", "params": { "project_id": 1 } }
      ]
    }
  }
}
