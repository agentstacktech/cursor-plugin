---
name: agentstack-sdk-surface
description: Print TypeScript SDK entrypoints — sdk.protocol, getCapabilityMatrix, and cache invalidation pointers.
---

# /agentstack-sdk-surface

Emit for the user's stack:

```ts
import { createSDK } from '@agentstack/sdk';
// sdk.protocol — REST + protein bus + snapshot cache
// getCapabilityMatrix() — stable AI slice
```

Link `docs/AGENT_PROTOCOL_QUICKSTART.md` and `docs/SDK_AI_SURFACE.md`.

Gene: `repo.platform.sdk.ai_surface.gen1`
