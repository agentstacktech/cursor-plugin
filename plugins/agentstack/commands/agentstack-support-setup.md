---
name: agentstack-support-setup
description: Enable project support channel, staff eligibility, and optional AI binding for support threads.
---

# /agentstack-support-setup

1. `social.support.eligibility` for target `project_id`.
2. Configure channel per platform docs (`core.social.support.gen1`).
3. If AI agent required, bind fleet agent with `agents.*` + support caps.

Verify with `/agentstack-diagnose`.
