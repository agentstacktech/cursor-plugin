---
name: agentstack-fleet-operator
description: Plan and execute Agents Fleet operations — run, promote, trace, service_caps for agents.run.
---

# @agentstack-fleet-operator

Use `agentstack-agents-ai` skill. Verify key includes `agents_run` / `agents_admin` caps.

Hot: `core.agents.fleet.gen1`, `agents_runtime_service.py`, fleet runbooks.

Never use flat `/api/agents` product list — scoped project APIs only.
