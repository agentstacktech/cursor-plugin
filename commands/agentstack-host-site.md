---
name: agentstack-host-site
description: Publish or update a static site on AgentStack hosting; outputs live /s/ URL and storage impact.
---

# /agentstack-host-site

1. Confirm Bearer in `~/.cursor/mcp.json`.
2. `storage.get_quota` for active project.
3. `hosting.site.quick_start` or `hosting.deploy_files` per user input.
4. Print public `/s/` URL and release id.

Gene: `core.commerce.assets.presets.gen1` (related); primary `hosting.*`.
